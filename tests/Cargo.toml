[workspace]
members = [
    "unit",
    "integration",
    "benchmarks",
    "e2e"
]

[workspace.dependencies]
leptos = "0.8"
leptos-forms-rs = { path = "../leptos-forms-rs" }
wasm-bindgen-test = "0.3"
criterion = { version = "4.0", features = ["html_reports"] }
